# Projects

The capability record: finished work that demonstrates a skill, not a learning exercise.

Every project runs on public or synthetic data only (see the repo `.gitignore`). Start a new one by copying `_TEMPLATE/`.

## Rule

Every project write-up states its method and its limitations. Results without either aren't done.
